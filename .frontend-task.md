You are the Frontend Agent. Build the 4 missing files for the PMS agent management UI.

The project is a Next.js app with Tailwind CSS and TypeScript.

Existing files to reference (READ THEM FIRST):
- lib/actions/agents.ts
- lib/actions/ai-models.ts
- components/agents/agent-card.tsx
- components/agents/agent-activity-feed.tsx
- components/agents/create-agent-dialog.tsx

Also read the sidebar and an existing dashboard page to match the design patterns.

Build these 4 files:

1. components/agents/agent-detail-panel.tsx
   - A slide-out panel showing agent details, status, model, activity feed, and edit capability
   - Uses agent-activity-feed.tsx for the activity section

2. components/agents/agent-list.tsx
   - A grid/list view of all agents using agent-card.tsx
   - Filters by squad (engineering/marketing/all) and status (online/busy/offline/idle)
   - Search by name

3. app/(dashboard)/agents/page.tsx
   - The main agents page
   - Uses agent-list and agent-detail-panel
   - Has a "Create Agent" button that opens create-agent-dialog

4. Update the sidebar to add an Agents link with a Bot icon from lucide-react

Use the existing design patterns from the project. Match the style of other dashboard pages.
