You are the Backend Agent. Your task is to push the agent management database migration to Supabase.

Use the Supabase MCP to execute the SQL migration.

Read the file: supabase/migrations/20260220000001_agents_system.sql

Then execute it against the database using the Supabase MCP. Break it into individual statements if needed.

Important: If types, tables, or functions already exist, handle gracefully (use IF NOT EXISTS, CREATE OR REPLACE, etc).

After running, verify by listing the new tables: agents, agent_activities, ai_models, agent_decisions.
Also verify the agent_id column was added to the tasks table.
