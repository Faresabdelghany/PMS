You are the Frontend Agent. Read the existing agent components in components/agents/ and lib/actions/agents.ts. Also read the sidebar and an existing dashboard page for design patterns. Then build these 4 files: First, components/agents/agent-detail-panel.tsx which is a slide-out panel with agent details and activity feed. Second, components/agents/agent-list.tsx which is a grid view with filters by squad and status. Third, app/(dashboard)/agents/page.tsx which is the main page using agent-list and detail-panel with Create Agent button. Fourth, update sidebar to add Agents link with Bot icon from lucide-react.